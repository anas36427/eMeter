# ============================================================
# eMeter AMU — Development-Only Requirements
# Install with: pip install -r requirements-dev.txt
# (already assumes requirements.txt is installed)
# ============================================================

-r requirements.txt

# --- Testing ---
pytest>=8.0.0
pytest-django>=4.8.0
pytest-cov>=5.0.0

# --- Code Quality ---
flake8>=7.0.0
black>=24.0.0

# --- Debug Toolbar (optional, safe for local only) ---
django-debug-toolbar>=4.3.0
